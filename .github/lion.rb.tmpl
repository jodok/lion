# typed: false
# frozen_string_literal: true

# This file is generated by lion's release workflow (.github/workflows/release.yml
# renders it from .github/lion.rb.tmpl). DO NOT EDIT in the tap.
class Lion < Formula
  desc "Task-first LinkedIn CLI: profile, connections, messaging, feed, local message store"
  homepage "https://github.com/jodok/lion"
  version "@@VERSION@@"

  on_macos do
    if Hardware::CPU.arm?
      url "https://github.com/jodok/lion/releases/download/@@TAG@@/lion_@@TAG@@_darwin_arm64.tar.gz"
      sha256 "@@SHA_DARWIN_ARM64@@"
    end
    if Hardware::CPU.intel?
      url "https://github.com/jodok/lion/releases/download/@@TAG@@/lion_@@TAG@@_darwin_amd64.tar.gz"
      sha256 "@@SHA_DARWIN_AMD64@@"
    end
  end

  on_linux do
    if Hardware::CPU.arm? && Hardware::CPU.is_64_bit?
      url "https://github.com/jodok/lion/releases/download/@@TAG@@/lion_@@TAG@@_linux_arm64.tar.gz"
      sha256 "@@SHA_LINUX_ARM64@@"
    end
    if Hardware::CPU.intel? && Hardware::CPU.is_64_bit?
      url "https://github.com/jodok/lion/releases/download/@@TAG@@/lion_@@TAG@@_linux_amd64.tar.gz"
      sha256 "@@SHA_LINUX_AMD64@@"
    end
  end

  def install
    # Homebrew extracts the tarball and changes into its single top-level
    # directory (lion_<version>_<os>_<arch>/), so the binary is right here.
    bin.install "lion"
  end

  test do
    assert_match "lion v#{version}", shell_output("#{bin}/lion version")
  end
end
